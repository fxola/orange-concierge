services:
  web:
    environment:
      AI_PROVIDER: ${AI_PROVIDER:-gemini}
      AI_MODEL: ${AI_MODEL:-gemini-1.5-flash}
      AI_TIMEOUT_MS: ${AI_TIMEOUT_MS:-30000}
      AI_BASE_URL: ${AI_BASE_URL:-https://generativelanguage.googleapis.com/v1beta}
      AI_API_KEY: ${AI_API_KEY:-}
